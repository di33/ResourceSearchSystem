# ============================================================
# Client 配置
# ============================================================
# 生成预览、生成描述、用途分类等 ResourceProcessor 配置已移到 ../Tools/.env。
# 相关私有密钥放在本目录的 .env.local。
# 对象存储上传是独立工具配置，由 client 调用 Tools/ObjectStorageUpload。
# 真实 AK/SK 放在 client/.env.local。

TEST_SERVER_URL=http://localhost:8000

CLIENT_ID=resource-crawler
RP_PROCESSING_SERVER_URL=http://125.88.194.226:9000
PREVIEW_RENDERER_URL = "http://localhost:8200"
